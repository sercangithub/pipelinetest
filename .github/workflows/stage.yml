name: pipeline
on: 
  push: 
    branches: [ stage ]        
env:
  test: test
jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: print env
        run: |
          echo "hi from main"
