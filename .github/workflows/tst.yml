name: pipeline
on:
  pull_request:
    branches: [ main ]
    types: [ closed ]        
env:
  test: test
jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: print env
        if: github.event.pull_request.merged == false
        run: |
          echo $GITHUB_REF_NAME
          echo hi
