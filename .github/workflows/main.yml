name: pipeline
on: 
  push: 
    branches: [ main ]        
env:
  test: test
  TOKEN: ${{ secrets.TOKEN }}
jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: print env
        run: |
          pip install requests
          pip install beautifulsoup4 
      - name: run python
        run: |
          python3 deneme.py
          
