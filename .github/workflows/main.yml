name: pipeline
on: 
  push: 
    branches: [ main ]        
env:
  test: test
jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: print env
        run: |
          pip install requests
          pip install BeautifulSoup 
          
